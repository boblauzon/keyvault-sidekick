# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
*.swp
*.swo

# Local dev notes
*.local.md

# Local test artifacts — never commit potential vault backups or saved copies
Test Files/
*.vault
keyvault-sidekick.html
keyvault-backup-*.html

# Node / wrangler
node_modules/
.wrangler/
*.local.toml

# Bootstrap output
.superadmin-bootstrap.json

# MCP local vault + test artifacts (never commit a real or test vault file)
mcp/.test-vault*.json
vault.json

# Local archive (e.g. the Designs/ design source moved out of the published repo)
_backup/
