{
  "name": "keyvault-sidekick",
  "version": "3.5.1",
  "description": "Browser-only AES-256-GCM encrypted secrets vault for vibe coders. Free, open, local-first — zero backend, no accounts. One-click handoff to Claude Code & ChatGPT Codex.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev public --port 8091",
    "deploy": "wrangler pages deploy public --project-name keyvault-sidekick --branch main"
  },
  "devDependencies": {
    "wrangler": "4.98.0"
  }
}
